#！/usr/bin/python
# -*- coding: utf-8 -*-
#运动命令
#forward(d)/backward(d)  向前向后移动d长度
#绘图窗口的原点（0，0）在正中间，默认海龟方向是右侧
#right(d)  向右转动多少度 left(d)
#goto(x,y)  移动到坐标为（x,y）的位置
#speed（speed）   笔画绘制的速度[0,10]
#笔画控制命令
#up()   笔画抬起，在移动的时候不会绘图
#down（）  笔画落下，移动绘图
#setheading(d)   改变海龟的朝向
#pensize()  笔画的宽度
#pencolor（colorstr）  笔画颜色
#reset()  恢复所有设置，清空窗口，重置turtle状态
#clear()  清空窗口，不会重置turtle
#circle(r,e)  绘制一个圆形，r为半径，e为次数
# turtle.circle(50,steps=5)
#begin_fill()
#fillcolor(colorstr)
#end_fill()    结束填充
#笔画控制命令
#其他命令
#done()  程序继续在执行
#undo()  撤销上一次动作
#hideturtle()  隐藏海龟
#showturtle()  显示海龟
#screensize(x,y)